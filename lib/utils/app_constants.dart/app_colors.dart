import 'package:flutter/material.dart';

class AppColors {
  // Base
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF060606);

  // Surfaces
  static const Color lightSurface = Color(0xFFF6F7F9);
  static const Color darkSurface = Color(0xFF121212);

  // Cards
  static const Color lightCard = Color(0xFFFFFFFF);
  static const Color darkCard = Color(0xFF1A1A1A);

  // Primary Accent (lime / blue from images)
  static const Color lime = Color(0xFFE6F05A);
  static const Color blue = Color(0xFF3D4BFF);

  // Text
  static const Color textPrimaryLight = Color(0xFF111111);
  static const Color textSecondaryLight = Color(0xFF6B6B6B);

  static const Color textPrimaryDark = Color(0xFFFFFFFF);
  static const Color textSecondaryDark = Color(0xFF9E9E9E);

  // States
  static const Color success = Color(0xFF3CB371);
  static const Color error = Color(0xFFE05A47);

  // Divider
  static const Color dividerLight = Color(0xFFEAEAEA);
  static const Color dividerDark = Color(0xFF2A2A2A);
}
